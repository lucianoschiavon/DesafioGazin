# Descricao

## API - Endpoints
(o que enviar) -> (formato padrão da resposta)

Isso aumenta um pouco o código, mas deixa:

manutenção mais fácil;

testes unitários possíveis;

leitura mais limpa;

aderência ao padrão MVC (Model–View–Controller).